#cloud.ui.basic.segment.center.aligned.container
    if selected_tribe_tags
        #clear_tags.ui.black.large.icon.compact.button
            i.remove.icon
        each selected_tribe_tags
            .unselect_tag.ui.black.large.compact.button
                strong #{this}
    each all_tags
        .ui.compact.button.select_tag(class=cloud_tag_class) #{this.name}
